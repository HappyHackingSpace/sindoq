version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - misspell
    - revive
  settings:
    gocritic:
      disabled-checks:
        - exitAfterDefer
    revive:
      rules:
        - name: exported
          disabled: true
        - name: unexported-return
          disabled: true

formatters:
  enable:
    - gofmt
